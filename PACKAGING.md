# パッケージング・配布ガイド

このドキュメントでは、入金消込アプリを実行ファイル（パッケージ）として配布する方法を説明します。

## 📦 生成できるパッケージ形式

### Windows
- **NSIS インストーラ** (`.exe`) - 通常のインストーラ
- **ポータブル版** (`.exe`) - インストール不要、USBメモリで持ち運び可
- **ZIP アーカイブ** (`.zip`) - 解凍して実行

### macOS
- **DMG** (`.dmg`) - macOS標準のインストーラ
- **ZIP アーカイブ** (`.zip`) - 解凍して実行

### Linux
- **AppImage** (`.AppImage`) - 全Linuxディストリビューション対応
- **DEB パッケージ** (`.deb`) - Debian/Ubuntu系
- **RPM パッケージ** (`.rpm`) - RedHat/Fedora系

---

## 🚀 パッケージの作成方法

### Windows用パッケージを作成

```bash
# Windows環境で実行
npm run dist:win
```

**生成されるファイル（`release/`フォルダ内）:**
- `入金消込アプリ Setup 1.0.0.exe` - インストーラ版
- `入金消込アプリ-1.0.0-portable.exe` - ポータブル版（推奨）
- `入金消込アプリ-1.0.0-win.zip` - ZIP版

### macOS用パッケージを作成

```bash
# macOS環境で実行
npm run dist:mac
```

**生成されるファイル:**
- `入金消込アプリ-1.0.0.dmg` - インストーラ版
- `入金消込アプリ-1.0.0-mac.zip` - ZIP版

### Linux用パッケージを作成

```bash
# Linux環境で実行
npm run dist:linux
```

**生成されるファイル:**
- `入金消込アプリ-1.0.0.AppImage` - AppImage版（推奨）
- `入金消込アプリ_1.0.0_amd64.deb` - Debian/Ubuntu用
- `入金消込アプリ-1.0.0.x86_64.rpm` - RedHat/Fedora用

### 全プラットフォーム用を一度に作成

```bash
# 注意: 各OSでしかビルドできないものもあります
npm run dist:all
```

---

## 📋 推奨パッケージ形式

### 社内配布の場合

| OS | 推奨形式 | 理由 |
|---|---------|------|
| **Windows** | **ポータブル版 (.exe)** | インストール不要、管理者権限不要 |
| **macOS** | **DMG** | macOS標準、信頼性が高い |
| **Linux** | **AppImage** | ディストリビューション非依存 |

### 個人利用の場合

| OS | 推奨形式 | 理由 |
|---|---------|------|
| **Windows** | **ZIP版** | 解凍するだけ、最も柔軟 |
| **macOS** | **ZIP版** | 解凍するだけ |
| **Linux** | **AppImage** | 実行権限を付与するだけ |

---

## 🎯 各パッケージの使用方法

### Windows

#### インストーラ版 (`.exe`)
1. ダブルクリックして実行
2. インストール先を選択
3. 「インストール」をクリック
4. デスクトップのショートカットから起動

#### ポータブル版 (`.exe`)
1. ダブルクリックするだけ
2. **インストール不要！**
3. USBメモリに入れて持ち運び可能

#### ZIP版 (`.zip`)
1. ZIPファイルを解凍
2. `入金消込アプリ.exe` をダブルクリック

### macOS

#### DMG版 (`.dmg`)
1. DMGファイルをダブルクリック
2. アプリを「アプリケーション」フォルダにドラッグ
3. Launchpadから起動

#### ZIP版 (`.zip`)
1. ZIPファイルを解凍
2. `入金消込アプリ.app` をダブルクリック

### Linux

#### AppImage版 (`.AppImage`)
```bash
# 実行権限を付与
chmod +x 入金消込アプリ-1.0.0.AppImage

# 実行
./入金消込アプリ-1.0.0.AppImage
```

#### DEB版 (`.deb`)
```bash
sudo dpkg -i 入金消込アプリ_1.0.0_amd64.deb
```

#### RPM版 (`.rpm`)
```bash
sudo rpm -i 入金消込アプリ-1.0.0.x86_64.rpm
```

---

## 💡 よくある質問

### Q1: どのパッケージを配布すればいい？

**A:** 用途によります：

- **社内の経理部門**: ポータブル版 (Windows) がおすすめ
  - インストール不要
  - 管理者権限不要
  - セットアップが簡単

- **一般ユーザー**: インストーラ版がおすすめ
  - 正式なインストール手順
  - アンインストールも簡単
  - スタートメニューに登録

### Q2: パッケージサイズはどのくらい？

約 150-200MB です（Electronランタイムを含むため）

### Q3: オフライン環境で使える？

**はい！** このアプリは完全オフラインで動作します。
- インターネット接続不要
- クラウド連携なし
- 全てローカルで完結

### Q4: パッケージ作成に必要な環境は？

| パッケージ | 必要な環境 |
|----------|----------|
| Windows用 | Windows 7以上 |
| macOS用 | macOS 10.13以上 |
| Linux用 | Ubuntu 18.04以上（推奨） |

**注意**: 基本的に、各OSでしか対応するパッケージを作成できません。
（例: WindowsインストーラはWindows環境でのみ作成可能）

### Q5: クロスプラットフォームビルドは可能？

一部可能ですが、推奨しません：
- **Windows → Linux**: 可能（AppImage、deb、rpm）
- **macOS → Windows**: 不可
- **Linux → macOS**: 不可

各OS環境で個別にビルドすることを推奨します。

### Q6: GitHub Actionsで自動ビルドできる？

**可能です！** CI/CDパイプラインで自動ビルド・リリースを設定できます。
（別途 `.github/workflows/build.yml` の設定が必要）

---

## 🔧 トラブルシューティング

### ビルドが失敗する

```bash
# キャッシュをクリア
npm cache clean --force

# node_modulesを再インストール
rm -rf node_modules
npm install

# 再ビルド
npm run build
npm run dist:win
```

### パッケージサイズが大きい

これは正常です。Electronアプリは以下を含むため大きくなります：
- Node.js ランタイム
- Chromium ブラウザエンジン
- アプリケーション本体

最適化したい場合:
- `electron-builder.config.js` で除外設定
- 不要な依存関係を削除
- asar パッケージを有効化

### Windows Defender / ウイルス対策ソフトが警告を出す

新しい実行ファイルは「署名なし」として警告されることがあります。

**対策:**
1. **コード署名証明書を購入** (有料、年間数万円)
2. **実行を許可** するよう社内に周知
3. **除外設定** をウイルス対策ソフトに追加

---

## 📤 配布方法

### 1. ファイル共有
- 社内ファイルサーバー
- Googleドライブ / OneDrive
- USBメモリ

### 2. GitHub Releases
```bash
# GitHubのReleasesページにアップロード
# または GitHub Actionsで自動リリース
```

### 3. 社内Webサーバー
```bash
# 社内イントラネットからダウンロード可能にする
```

---

## 🎉 推奨配布パッケージまとめ

**最も簡単な配布方法:**

```bash
# Windowsユーザー向け
npm run dist:win
→ release/入金消込アプリ-1.0.0-portable.exe を配布

# macOSユーザー向け  
npm run dist:mac
→ release/入金消込アプリ-1.0.0.dmg を配布

# Linuxユーザー向け
npm run dist:linux
→ release/入金消込アプリ-1.0.0.AppImage を配布
```

**配布後の使い方:**
1. ファイルをダウンロード
2. ダブルクリック（Linuxは実行権限付与が必要）
3. すぐに使える！

---

詳細な設定は `package.json` の `build` セクションを参照してください。
